__pycache__/
*.pyc
.DS_Store
venv/
data/
dist/
packages/

# installed node packages live in subfolders under nodes/ (their source is in
# packages/); the lockfile is machine-specific. Core nodes are top-level .py
# files and stay tracked.
nodes/*/
nodes/.nbt-packages.json
